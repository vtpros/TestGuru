<h1><%= t('.header') %>:</h1>

<p>
  <%= link_to t('.new_test'), new_admin_test_path %>
</p>

<table>
  <thead>
    <tr>
      <th><%= t('app.title') %></th>
      <th><%= t('app.level') %></th>
      <th><%= t('app.questions') %></th>
      <th colspan='3'><%= t('app.actions') %>:</th>
    </tr>
  </thead>
  <tbody>
    <%= render @tests %>
  </tbody>
</table>
