<%= question_header(@test) %>
<%= form_with(model: model, local: true) do |f| %>
  <%= f.label(:'body', 'Question text:') %><br>
  <%= f.text_area(:'body') %><br>

  <%= f.submit("Send") %>
<% end %>
