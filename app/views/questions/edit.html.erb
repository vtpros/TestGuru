<%= question_header(@question.test, :edit) %>
<%= form_with(model: @question, local: true) do |f| %>
  <%= f.label(:'body', 'Question text:') %>:<br>
  <%= f.text_area(:'body') %><br>

  <%= f.submit("Send") %>
<% end %>
