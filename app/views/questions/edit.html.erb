<% if @question.errors.present? %>
  <%= render 'shared/errors/errors_list', errors: @question.errors %>
<% end %>
<% @test = @question.test %>

<%= render 'shared/question_form', model: @question %>
