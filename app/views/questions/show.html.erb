<h1>Question:</h1>
<p>
  <%= @question.body %>
  (<%= link_to 'edit', edit_question_path(@question) %>,
  <%= link_to 'delete',
              question_path(@question),
              method: :delete,
              data: { confirm: 'Are you sure?' }
   %>)
</p>
