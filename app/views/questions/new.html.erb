<%= question_header(@test, :create) %>
<%= form_with(model: [@test, @question]) do |f| %>
  <%= f.label(:'body', 'New question') %>:<br>
  <%= f.text_area(:'body') %><br>

  <%= f.submit("Send") %>
<% end %>
